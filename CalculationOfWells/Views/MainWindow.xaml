<Window x:Class="CalculationOfWells.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalculationOfWells.Views"
        xmlns:viewmodels="clr-namespace:CalculationOfWells.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d" Title="MainWindow" Height="450" Width="800">

    <Grid Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,2"  >
            <StackPanel.Resources>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Margin" Value="10,0,0,0"/>
                </Style>
            </StackPanel.Resources>
            <Button Content="Импорт" Command="{Binding ImportWellsCommand}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Margin" Value="0,0,0,0"/>
                        <Setter Property="IsEnabled" Value="False"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsBusy}" Value="False">
                                <Setter Property="IsEnabled" Value="True"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="Очистить" Command="{Binding ClearWellCommand}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Margin" Value="10,0,0,0"/>
                        <Setter Property="IsEnabled" Value="False"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsBusy}" Value="False">
                                <Setter Property="IsEnabled" Value="True"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <TextBlock Visibility="Hidden" Text="{Binding IsBusy, StringFormat=Busy: {0}}" Margin="100,0,0,0"/>
        </StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Summaries}" AutoGenerateColumns="True" IsReadOnly="True"/>
        <ListBox Grid.Row="2" ItemsSource="{Binding Errors}">
        </ListBox>
    </Grid>

</Window>
